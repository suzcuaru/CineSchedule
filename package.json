{
  "name": "cineschedule",
  "private": true,
  "version": "3.0.4",
  "type": "module",
  "scripts": {
    "dev": "node scripts/sync-version.js && vite",
    "build": "node scripts/sync-version.js && vite build",
    "preview": "node scripts/sync-version.js && vite preview",
    "sync-version": "node scripts/sync-version.js",
    "type-check": "tsc --noEmit",
    "lint": "echo 'Linting not configured yet'",
    "test": "echo 'Tests not configured yet'"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "lucide-react": "^0.554.0",
    "idb": "^8.0.3",
    "@google/genai": "^1.34.0"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "~5.8.2",
    "vite": "^6.2.0"
  }
}
